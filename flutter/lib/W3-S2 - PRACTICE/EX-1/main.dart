import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
      home: Scaffold(
          body: Center(
    child: Text(
      "Hello I'm Kao Vichet",
      style: TextStyle(
          fontSize: 30, color: Colors.blue, fontWeight: FontWeight.bold),
    ),
  ))));
}
